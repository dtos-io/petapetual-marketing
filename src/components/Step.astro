---
const { index, title } = Astro.props;
---

<div class="flex flex-row gap-2">
    <div
        class="bg-stone-200 shrink-0 font-bold rounded-full w-16 h-16 flex items-center justify-center text-xl"
    >
        {index}
    </div>
    <div class="flex flex-col gap-1">
        <div class="font-semibold">{title}</div>
        <div class=""><slot /></div>
    </div>
</div>
